import { getElement } from "./getElement.js";
export const time = getElement(".time");
// this btn on click changes to stopBtn
export const startBtn = getElement(".start");
export const settingsBtn = getElement(".settings");
export const minutes = getElement(".minutes input");
export const seconds = getElement(".seconds input");
export const ring = getElement(".ring");
